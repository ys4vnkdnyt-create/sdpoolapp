{
  "name": "sd-lap-lane-finder",
  "version": "0.1.0",
  "private": true,
  "description": "San Diego lap lane availability prototype",
  "type": "module",
  "scripts": {
    "build": "tsc && mkdir -p dist/data/pools && cp src/data/pools/pools.json dist/data/pools/pools.json",
    "start": "node dist/index.js",
    "dev": "npm run build && npm run start",
    "web": "npm run build && node dist/server.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
