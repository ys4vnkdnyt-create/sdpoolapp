{
  "name": "sd-lap-lane-finder",
  "version": "0.1.0",
  "private": true,
  "description": "Lap lane availability by region (San Diego first)",
  "type": "module",
  "scripts": {
    "build": "tsc && mkdir -p dist/data/pools && cp src/data/pools/*.json dist/data/pools/",
    "start": "node dist/index.js",
    "start:web": "node dist/server.js",
    "dev": "npm run build && npm run start",
    "web": "npm run build && node dist/server.js",
    "ingest": "npm run build && node dist/ingest/cli.js",
    "ingest:refresh": "npm run build && node dist/ingest/cli.js --region san-diego --refresh-pdfs",
    "tunnel": "bash scripts/tunnel.sh",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "pdf-parse": "^1.1.1",
    "pdf-to-img": "^6.2.0",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "posthog-js": "^1.379.0"
  }
}
