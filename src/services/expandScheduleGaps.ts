/**
 * Re-export for older imports — logic lives in scheduleWindows.ts.
 */
export {
  expandGapsBetweenBlocks as expandAvailabilityWithGaps,
  prepareAvailabilityForSearch,
} from "./scheduleWindows.js";
